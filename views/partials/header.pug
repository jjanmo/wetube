header.header
    .header_column
        .header_icon
            a(href=routes.home)
                i.fab.fa-youtube  #{siteName}
    .header_column 
        form(action=routes.search, method="get")
            .searching_bar
                input(type="text", name="term", placeholder="Enter searching term")
                button 
                    i.fas.fa-search
    .header_column
        ul
            li
                a(href=routes.join) Join
            li
                a(href=routes.login) LogIn    