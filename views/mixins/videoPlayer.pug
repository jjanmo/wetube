mixin videoPlayer( video = {})
    .videoPlayer#jsVideoPlayer
        video#jsVideo(src=`/${video.file}`)
        .progressBar#jsProgressBar
        .videoPlayerControl#jsVideoController
            .videoPlayerColumn
                span#jsPlayBtn
                    i.fas.fa-play
                span 
                    span#jsCurrentTime 00:00:00 / 
                    span#jsTotalTime 00:00:00
            .videoPlayerColumn
                span#jsVolumeBtn
                    i.fas.fa-volume-up 
                span
                    input#jsVolumeBar(type="range" max="1" min="0" step="0.01" value="1")
            .videoPlayerColumn
                span#jsFullScreenBtn
                    i.fas.fa-expand

    
